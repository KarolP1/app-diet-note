import React from "react";
import { SettingsContainer } from "./DisplaySettings.styled";

const DisplaySettings = () => {
	return <SettingsContainer>settings</SettingsContainer>;
};

export default DisplaySettings;
